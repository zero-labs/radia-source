<% 
	@pagetitle = "Edit program: #{@program.name}" 
	add_crumb("Programs", programs_path)
	add_crumb("#{@program.name}", program_path(@program), true)
	add_crumb("Edit", edit_program_path(@program))
%>

<% content_for :sidebar do %>
<% end %>

<% form_for :program, @program, :url => { :action => 'update' }, :html => { :method => :put } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>