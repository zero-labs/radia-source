<% remote_form_for(@bloc_element, :url => create_bloc_element_path(@emission_type), :html => { :method => :put }) do |f| %>
<%= hidden_field_tag 'bloc_element[asset][kind]', 'single' %>
<fieldset>
	<legend>Single element</legend>
	
	<%= render :partial => 'authored', :locals => { :single => true } %>
	<%= render :partial => 'duration', :locals => { :f => f } %>
	
</fieldset>
<% end %>
