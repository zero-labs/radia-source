<%= hidden_field_tag 'single[asset_service_id]', service_id %>
<ul id="files">
<% files.each do |f| %>
	<li>
		<%= radio_button_tag 'single[partial_retrieval_uri]', f, false, :id => f.hash %>
		<label for="<%= f.hash %>"><%= f %></label>
	</li>
<% end %>
</ul>